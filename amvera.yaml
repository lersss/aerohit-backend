meta:
  environment: node
  toolchain:
    name: npm
    version: 18
build:
  additionalCommands:
    - npm install
    - npx prisma generate
  artifacts:
    "*": /
run:
  scriptName: src/index.js
  persistenceMount: /data
