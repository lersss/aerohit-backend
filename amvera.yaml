meta:
  environment: node
  toolchain:
    name: npm
    version: 18
build:
  additionalCommands:
    - npm install && npx prisma generate
  artifacts:
    "*": /
run:
  command: node src/index.js
  containerPort: 5000
  persistenceMount: /data
