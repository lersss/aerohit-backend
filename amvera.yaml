meta:
  environment: node
  toolchain:
    name: npm
    version: 18
build:
  additionalCommands:
    - npm install
    - npx prisma generate
    - npx prisma db push
  artifacts:
    "*": /
run:
  command: npm start
  containerPort: 80
  persistenceMount: /data
